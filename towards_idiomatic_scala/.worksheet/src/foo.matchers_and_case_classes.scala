package foo

object matchers_and_case_classes {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(91); 
  println("Welcome to the Scala worksheet")}
  
  class User(val id: Int, val name: String)
  case class Channel(id: Int, name: String)
  
  
}