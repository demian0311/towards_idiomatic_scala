object foo {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(201); 

    val asdf: Array[Array[Option[Int]]] = Array(
        Array(Some(201), Some(113), None),
        Array(Some(761), Some(942), Some(683)),
        Array(None,      Some(103), Some(824)));System.out.println("""asdf  : Array[Array[Option[Int]]] = """ + $show(asdf ))}
        
        
        
}